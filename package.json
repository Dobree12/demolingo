{
  "name": "mom-duo2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "verify": "node scripts/verify-vocab.mjs",
    "build": "npm run verify && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
